services:
  - type: web
    name: cartelera-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free